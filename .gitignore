# Dependencies
node_modules/

# Env (never commit secrets)
.env
.env.local
.env.*.local

# Next.js
.next/
out/

# OS / IDE
.DS_Store
*.log
client_secret_*.json
googlauth.txt

# User-uploaded media (local dev / simple hosting)
public/uploads/

# TLS / Certbot — NEVER commit certificates or private keys
certbot/
*.pem
*.key
*.crt
private_key.json

# Build artifacts
tsconfig.tsbuildinfo

